# what is != and where should you use it?
# put ! before something, like !user_name
# put ! after something, like words.uniq!
# put ? before something
# put ? after something
# put !! before something, like !!user_name

# != means not equal to
# ! turns object into boolean
# ! after object destructively modifies it
# ? : is the ternary if..else operator
# ? at the end of a method name could mean anything.
# It would be part of the method name.
# !! converts the object into a boolean value then reverses
# it's boolean value.